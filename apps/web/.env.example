# Client-side salt for password hashing (generate with: openssl rand -hex 32)
VITE_CLIENT_SALT=your-64-char-hex-salt-here

# Relayer base URL (for production build; dev uses proxy)
# VITE_RELAYER_URL=https://your-relayer.onrender.com
