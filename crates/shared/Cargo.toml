[package]
name = "shared"
version = "0.1.0"
edition = "2021"
description = "Shared types and models for relayer and executor"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
